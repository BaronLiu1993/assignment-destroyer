# Create tools with them such as MCP
def web_search_tool(query):
    pass

# Generate subagents based on the plan purpose, only if they can run in parallel
def generate_subagent(user_id, thread_id, purpose, context):
    pass
